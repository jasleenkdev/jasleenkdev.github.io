# audio

`click.mp3` — the short, quiet click/pop played on every click anywhere on the
page (see `<audio id="click-sfx">` in `index.html`, and section 7 of
`js/scrapbook.js`). Drop a royalty-free click here under this exact name.

Background music is *not* a file: it is the hidden YouTube embed `#bg-music`.
